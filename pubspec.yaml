name: pulsewise
description: "A consumer wellness app for daily logs, routines, and wearable insights."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+5

environment:
  sdk: ^3.5.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # # The following adds the Cupertino Icons font to your application.
  # # Use with the CupertinoIcons class for iOS style icons.
  # cupertino_icons: ^1.0.8
  # flutter_riverpod: ^2.6.1
  # go_router: ^15.1.2
  # dio: ^5.9.2
  # flutter_dotenv: ^6.0.0
  # json_annotation: ^4.9.0
  # hive: ^2.2.3
  # hive_flutter: ^1.1.0
  # flutter_secure_storage: ^10.0.0
  # heroicons: ^0.11.0
  # fluentui_system_icons: ^1.1.273
  # another_flushbar: ^1.12.32
  # file_picker: ^10.3.10
  # open_file: ^3.5.11
  # flutter_pdfview: ^1.4.4
  # syncfusion_flutter_pdfviewer: ^29.1.38
  # device_info_plus: ^11.3.0
  # permission_handler: ^12.0.1
  
  flutter_riverpod: ^2.5.1
  flutter_displaymode: ^0.6.0
  path_provider: ^2.1.4 # Can be removed if not needed
  easy_localization: ^3.0.7 # Can be removed if not needed
  hive: ^2.2.3 # Can be removed if not needed
  hive_flutter: ^1.1.0 # Can be removed if not needed
  url_launcher: ^6.3.0 # Can be removed if not needed
  fluentui_system_icons: ^1.1.255 # Can be removed if not needed
  flutter_staggered_grid_view: ^0.7.0 # Can be removed if not needed
  flutter_pdfview: 1.3.2 # Can be removed if not needed
  permission_handler: ^11.3.1
  device_info_plus: ^10.1.2
  package_info_plus: ^8.0.2
  shared_preferences: ^2.5.3

  # API Integration dependencies
  dio: ^5.4.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  table_calendar: ^3.1.3
  heroicons: ^0.11.0
  highcharts_flutter: ^1.2.0
  file_picker: ^8.1.6
  flutter_dotenv: ^5.1.0
  intl: ^0.19.0
  another_flushbar: ^1.12.30
  open_file: ^3.5.7
  flutter_html: ^3.0.0-beta.2
  markdown: ^7.3.0
  syncfusion_flutter_pdfviewer: ^27.1.53
  go_router: ^15.1.2

  # Reporting dependencies
  pdf: ^3.10.4
  printing: ^5.11.0
  excel: ^4.0.0
  share_plus: ^10.0.0
  jwt_decoder: ^2.0.1
  firebase_core: ^4.3.0
  firebase_messaging: ^16.1.0
  flutter_local_notifications: ^19.5.0
  flutter_svg: ^2.1.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.2.3
  uuid: ^4.5.3
  flutter_health_connect: ^1.2.3
  skeletonizer: ^1.4.3
  flutter_contacts: ^1.1.9+2
  google_sign_in: ^6.2.1
  google_sign_in_platform_interface: ^2.5.0
  google_sign_in_web: ^0.12.4+4
  image_cropper: ^8.0.2
  fl_chart: ^0.66.0
  syncfusion_flutter_gauges: ^27.2.5
  camera: ^0.11.0+2
  calendar_date_picker2: ^1.1.9
  day_night_time_picker: ^1.3.1
  time_picker_spinner_pop_up: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  build_runner: ^2.4.13
  json_serializable: ^6.9.0
  flutter_native_splash: ^2.4.1

dependency_overrides:
  image_cropper_platform_interface: 7.1.0

flutter_native_splash:
  color: "#FFFFFF"
  image: assets/images/group_8.png
  android: true
  ios: true
  web: true
  android_12:
    color: "#FFFFFF"
    image: assets/images/android12_splash_icon.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/svgs/
    - assets/images/
    - .env

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Outfit
      fonts:
        - asset: assets/fonts/Outfit-Thin.ttf
          weight: 100
        - asset: assets/fonts/Outfit-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/Outfit-Light.ttf
          weight: 300
        - asset: assets/fonts/Outfit-Regular.ttf
          weight: 400
        - asset: assets/fonts/Outfit-Medium.ttf
          weight: 500
        - asset: assets/fonts/Outfit-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Outfit-Bold.ttf
          weight: 700
        - asset: assets/fonts/Outfit-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/Outfit-Black.ttf
          weight: 900
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
